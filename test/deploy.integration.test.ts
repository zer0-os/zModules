

describe("zModules Deploy Integration Test", () => {
  describe("Staking", () => {
    it("Should deploy StakingERC20 with zDC and default config", () => {
      // 1. getConfig()
      // 2. run campaign function
      // 3. check state values after deploy against default values in staking20Config
    });
  });
});
